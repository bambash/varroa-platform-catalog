# Dummy credential fixtures for platform test bundles. These are NOT real
# secrets — they exist so pipelines and folder stores have credentials to
# reference in test controllers.
credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: ci-bot
              username: ci-bot
              password: "dummy-not-a-real-secret"
              description: "Dummy CI bot credential (catalog test fixture)"
          - string:
              scope: GLOBAL
              id: sonar-token
              secret: "dummy-not-a-real-secret"
              description: "Dummy Sonar token (catalog test fixture)"
