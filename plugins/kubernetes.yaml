plugins:
  # Versions are intentionally unpinned: the JenkinsVersionProfile plugin-set
  # lock owns kubernetes plugin versions. Pinning here re-introduces the
  # "plugin version conflict" reconcile failure whenever the lock is bumped.
  - artifactId: kubernetes
  - artifactId: kubernetes-credentials
